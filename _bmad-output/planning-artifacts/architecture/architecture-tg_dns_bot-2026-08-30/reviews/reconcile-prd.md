# تطبیق محدود PRD با Architecture Spine

**نتیجه: FAIL — چهار شکاف باربر**

1. **مرز دسترسی عمومی و نبود allowlist کاربر در spine ثبت نشده است.** PRD صریحاً تصمیم میگیرد هرکس به بات دسترسی دارد بتواند از قابلیتهای فعال استفاده کند و نسخهٔ نخست فهرست مجاز کاربر نداشته باشد (`prd.md:24`، `263`). Spine محدودسازی نرخ را تعریف میکند، اما این مرز مجوز محصول را الزامآور نکرده است؛ پیادهسازی میتواند ناخواسته احراز هویت یا allowlist اضافه کند.

2. **تقسیم پاسخ بلند خواندن به پیامهای شمارهگذاریشدهٔ تلگرام غایب است.** `FR-8` الزام میکند پاسخ فراتر از ظرفیت خروجی تلگرام به چند پیام شمارهگذاریشده تقسیم شود (`prd.md:163`). Spine حالتهای خواندن و بازسازی را مشخص میکند (`ARCHITECTURE-SPINE.md:160-164`) ولی قرارداد تحویل خروجی بلند را ندارد.

3. **نمایش همیشگی رکوردهای ناشناس در کنار پیام مدیریتشده تضمین نشده است.** PRD میخواهد رکوردهای ناشناس بدون حذف محتوا و بهصورت خام نمایش داده شوند (`prd.md:37`، `151`، `155-162`، `270`). `AD-16` فقط در حالت «پیام ناقص» نمایش تمام RRهای خام را الزام میکند (`ARCHITECTURE-SPINE.md:164`) و برای حالت «یک پیام کامل» حفظ و نمایش RRهای ناشناس را تصریح نمیکند.

4. **حداقلسازی دامنهٔ مجوز توکن کلودفلر ثبت نشده است.** PRD و پیوست، محدودکردن توکن به DNS Edit فقط برای ناحیههای ضروری را الزام امنیتی میدانند (`prd.md:199-200`؛ `addendum.md:76-78`). Spine نوشتن برنامه را به دامنههای مجاز محدود میکند، اما قید مستقل least-privilege روی خود credential/Cloudflare token را الزامآور نکرده است.
