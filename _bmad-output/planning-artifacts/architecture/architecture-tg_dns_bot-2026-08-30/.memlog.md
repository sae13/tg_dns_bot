---
scope: بات تلگرام، قرارداد پیام متنی دامنه و مرزهای زیرساختی نسخهٔ نخست
purpose: build-substrate
altitude: feature
updated: 2026-08-30T11:47
---

- (direction) مسیر سریع و فقط ستون فقرات؛ خروجی توضیحی جداگانه لازم نیست.
- (constraint) PRD نهایی و پیوست فنی آن ورودی الزامآور رفتار محصول هستند.
- (version) نسخههای زندهٔ PyPI در 2026-08-30 بررسی شدند: Python 3.13.15، aiogram 3.31.0، cloudflare 5.6.0، dnspython 2.8.0، pydantic-settings 2.15.0، structlog 26.1.0، pytest 9.1.1 و ruff 0.16.5.
- (decision) AD-1 معماری ششضلعی با هستهٔ کاربرد مستقل از تلگرام، کلودفلر و حلکنندهٔ DNS؛ جلوگیری از قفلشدن منطق به SDKها.
- (decision) AD-2 فرمانهای تلگرام فقط adapter ورودی هستند و use caseهای SendMessage، ReadInbox و ShowHelp را فراخوانی میکنند.
- (decision) AD-3 یک MailboxName canonical مالک یک جریان mutation است؛ ارسالهای همزمان هر صندوق داخل همان پردازه با قفل کلیددار سری میشوند.
- (decision) AD-4 رکورد اصلی manifest commit point است؛ قطعههای شمارهدار جدید ابتدا، manifest آخر، پاکسازی قطعههای قدیمی پس از commit؛ خواننده فقط مجموعهٔ کامل همشناسه را معتبر میداند.
- (decision) AD-5 فقط CloudflareRecordStore اجازهٔ mutation دارد؛ PublicTxtResolver فقط read-only است و inbox از DNS عمومی میخواند.
- (decision) AD-6 payload نسخهدار با UTF-8، timestamp UTC، Telegram user id اجباری و username اختیاری؛ نمایش زمان در Asia/Tehran.
- (decision) AD-7 تنظیمات typed و fail-fast؛ secrets فقط environment، domain allowlist canonical، سه feature flag مستقل.
- (decision) AD-8 rate limit درونپردازهای بر کلیدهای sender و mailbox با پارامترهای config؛ deployment نسخهٔ نخست تک replica است.
- (decision) AD-9 خطاهای domain/application مستقل از SDK، نگاشت به پاسخ تلگرام در adapter؛ logging ساختاریافته بدون secret و message body.
- (decision) AD-10 long polling و یک process/container برای نسخهٔ نخست؛ startup config validation و graceful shutdown؛ state durable فقط DNS است.
- (decision) AD-11 تا AD-18 هشت شکاف مرور خصمانه را بستند و gate closure با 8 PASS و 0 FAIL عبور کرد.
- (decision) AD-19 تا AD-21 شکافهای تطبیق PRD را بستند: نبود allowlist، نمایش همیشگی raw RR و قطعهبندی خروجی تلگرام، و توکن Cloudflare با کمترین دسترسی.
- (change) بازبینی ساختار و نثر انجام شد؛ فهرست تکراری تنظیمات حذف و ابهامهای قراردادی اصلاح شدند.
- (event) ARCHITECTURE-SPINE finalized after deterministic lint, 8/8 adversarial closure and 4/4 PRD reconciliation closure.
